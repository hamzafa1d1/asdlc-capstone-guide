/* Core concepts + Common building blocks: edit content here; app.js renders it.
   Sources checked 25 Sep 2026. Keep dates on sources so readers can judge freshness. */
(function () {
  var P = (window.GUIDE_PARTS = window.GUIDE_PARTS || {});

  P.concepts = {
    id: "concepts",
    num: "04",
    title: "Core concepts",
    kicker: "Reading + one lab · about 6 h in week 1",
    summary: "What an agent, a harness and context engineering are in 2026, what changed in the last 12 months, and a lab that puts numbers on the difference between a workflow and an agent.",
    blocks: [
      { type: "lead", html: "Read the definitions today. Do the lab this week: it takes about 2 hours and gives you your first measured result. Work through the essential reading during week 1. Every source has a date. Most of what you need was written after September 2025, so treat anything older as background." },
      { type: "equation", html: "<span>Agent</span> <em>=</em> <span>Model</span> <em>+</em> <span>Harness</span>" },
      { type: "p", html: "The model is the part you rent. The harness is the part you build: the loop, the tools, the rules, the checks. Most of your grade depends on the harness, and most of the definitions below describe a piece of it. Where a term has a Claude Code equivalent, the link takes you to how Claude Code implements it." },

      { type: "h", text: "Definitions", id: "defs" },
      { type: "defs", items: [
        { term: "Context window and context rot",
          def: "The context window is everything the model sees on one call: system prompt, rule files, tool definitions, conversation history and tool results. You pay for all of it on every call. Accuracy drops as the window fills, long before the limit: Chroma tested 18 models in 2025 and all of them got less reliable as input length grew. They called this context rot.",
          src: { t: "Chroma · Context rot (2025)", url: "https://www.trychroma.com/research/context-rot" }, cc: "context" },
        { term: "Context engineering",
          def: "Choosing the smallest set of tokens that gives the model what it needs for its next step: instructions, tools, retrieved data, memory and history. Anthropic describes the window as a finite attention budget that every extra token spends. It replaced prompt engineering as the core skill once agents started running for dozens of turns. In Claude Code, <code>/context</code> shows you where the budget goes (see <a data-cc=\"context\">context management</a>).",
          src: { t: "Anthropic · Effective context engineering for AI agents (Sep 2025)", url: "https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents" }, cc: "context" },
        { term: "Workflow vs agent",
          def: "A workflow runs LLM calls along paths you wrote in code. An agent is a model using tools in a loop: it picks an action, the harness runs it, the result goes back into the context, and it picks again until it answers or a stop condition fires. Use a workflow when you can draw the steps in advance and an agent when you cannot. The lab on this page measures the difference.",
          src: { t: "Anthropic · Building effective agents (Dec 2024)", url: "https://www.anthropic.com/engineering/building-effective-agents" }, cc: "agent-loop" },
        { term: "Harness",
          def: "Everything in an agent except the model: the loop, the tools and their permissions, the sandbox, <a data-cc=\"claude-md\">rule files</a>, <a data-cc=\"skills\">skills</a>, <a data-cc=\"hooks\">hooks</a>, memory files, budgets and tracing. Böckeler (Thoughtworks) splits it into <em>guides</em> that steer the agent before it acts, such as rule files and skills, and <em>sensors</em> that check its work afterwards, such as tests, linters and review agents. The same model in a different harness gives very different results. You will measure that in your project.",
          src: { t: "Böckeler · Harness engineering for coding agent users (Apr 2026)", url: "https://martinfowler.com/articles/harness-engineering.html" }, cc: "settings" },
        { term: "Tools and tool design",
          def: "A tool is a function the harness runs when the model asks for it: read a file, run the tests, query a database. The model only sees the tool's name, description and input schema, so treat those as prompts. Anthropic's advice: a few tools that match real workflows instead of one per API endpoint, prefixed names, responses that return only high-signal fields, and pagination or truncation by default. Strict tool use (structured outputs) guarantees that calls match your schema.",
          src: { t: "Anthropic · Writing effective tools for agents (Sep 2025)", url: "https://www.anthropic.com/engineering/writing-tools-for-agents" }, cc: "tools" },
        { term: "MCP (Model Context Protocol)",
          def: "An open standard for exposing tools, resources and prompts to any agent: you write a server once and any MCP client can use it. The current spec is dated 2026-07-28. It made the protocol stateless (plain request/response, no session handshake), added a formal extensions framework that carries MCP Apps (interactive UI) and Tasks, and tightened OAuth-based authorization. Public servers are listed in the official MCP Registry. Since December 2025 the Agentic AI Foundation under the Linux Foundation governs MCP. Claude Code connects to MCP servers through <a data-cc=\"mcp\">MCP settings</a>.",
          src: { t: "MCP blog · The 2026-07-28 specification (Jul 2026)", url: "https://blog.modelcontextprotocol.io/posts/2026-07-28/" }, cc: "mcp" },
        { term: "Agent Skills",
          def: "A skill is a folder with a <code>SKILL.md</code> file (YAML front matter with a name and description, then instructions) and optional scripts and reference files. At startup the agent sees only each skill's name and description. It loads the rest when a task matches, which is called progressive disclosure. Agent Skills became an open standard in December 2025, and Claude Code, OpenAI Codex and GitHub Copilot all read the same format.",
          src: { t: "agentskills.io · Agent Skills specification", url: "https://agentskills.io/home" }, cc: "skills" },
        { term: "Rule file (AGENTS.md / CLAUDE.md)",
          def: "A Markdown file at the repo root that every session loads: build and test commands, conventions, and what the agent must never do. <code>AGENTS.md</code> is the cross-tool name. <code>CLAUDE.md</code> is Claude Code's own name, and Claude Code now reads <code>AGENTS.md</code> too. The file loads on every call, so keep it short (Claude Code's docs say under 200 lines) and move task-specific knowledge into skills. The model can ignore it, so enforce hard rules with a <a data-cc=\"hooks\">hook</a> or a <a data-cc=\"permissions\">permission rule</a>.",
          src: { t: "Claude Code docs · How Claude remembers your project", url: "https://code.claude.com/docs/en/memory" }, cc: "claude-md" },
        { term: "Subagents and multi-agent systems",
          def: "A subagent is a separate model call with its own clean context. It does one job and returns a short summary, which keeps the main context small. Anthropic's research system, a lead agent with parallel subagents, scored 90.2% higher than a single agent on their internal eval but used about 15 times the tokens of a chat. Cognition's 2026 update to its \"Don't build multi-agents\" post reaches a similar conclusion: extra agents help when they add intelligence (search, review) and all writes stay in one thread.",
          src: { t: "Anthropic · How we built our multi-agent research system (Jun 2025)", url: "https://www.anthropic.com/engineering/multi-agent-research-system" }, cc: "subagents" },
        { term: "Compaction and memory",
          def: "Long tasks outgrow any context window. Compaction replaces old history with a summary so the agent can keep going. Memory keeps state outside the window: a progress file, the git log, a memory directory the agent reads at the start of the next session. Anthropic's long-running harness uses an initializer session that writes <code>feature_list.json</code>, <code>claude-progress.txt</code> and <code>init.sh</code>. Each later session then finishes one feature, tests it and commits. Claude Code has <a data-cc=\"context\">/compact</a> and <a data-cc=\"memory\">auto memory</a>.",
          src: { t: "Anthropic · Effective harnesses for long-running agents (Nov 2025)", url: "https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents" }, cc: "memory" },
        { term: "Reasoning and thinking",
          def: "Current models can reason before they answer and between tool calls (interleaved thinking). On current Claude models thinking is adaptive: the model decides per request whether to think and how much, and you steer it with an <code>effort</code> level from <code>low</code> to <code>max</code>. Thinking tokens are billed as output, so effort trades cost and latency against quality. Treat an effort change like any other change and check it on your evals.",
          src: { t: "Claude docs · Steering thinking", url: "https://platform.claude.com/docs/en/build-with-claude/thinking-steering-and-cost" }, cc: "cost-tracking" },
        { term: "RAG, agentic search and long context",
          def: "RAG (retrieval-augmented generation) embeds your documents, retrieves the closest matches for a query and puts them in the context before the model answers. Agentic search gives the agent search tools (grep, file reads, a search API) and lets it look things up step by step, when it needs them. Claude Code loads <code>CLAUDE.md</code> up front and then uses glob and grep to pull in files as it goes. A long context window does not make either approach unnecessary: context rot means that filling the window costs accuracy as well as money.",
          src: { t: "Anthropic · Effective context engineering (Sep 2025)", url: "https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents" }, cc: "tools" },
        { term: "Evals for agents",
          def: "A fixed set of tasks, a way to grade each result, and a score you track over time. For agents, grade the outcome (the tests pass, the right record changed) and not the exact sequence of steps. Anthropic suggests starting with 20 to 50 tasks taken from real failures. Mix code-based, model-based and human graders, and read the transcripts. If users need the agent to be consistent, report pass^k (all k runs succeed) instead of pass@k (at least one of k runs succeeds). Run the suite in CI with <a data-cc=\"headless\">headless mode</a>.",
          src: { t: "Anthropic · Demystifying evals for AI agents (Jan 2026)", url: "https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents" }, cc: "headless" },
        { term: "LLM-as-judge",
          def: "Using a model to grade what code cannot check, such as \"is this review comment correct and useful?\". A judge is a classifier, so validate it before you trust its scores. Label a held-out set of outputs yourself, run the judge on the same set, and report its true positive rate and true negative rate separately. Raw agreement hides errors when most items pass. Prefer a pass/fail verdict with a written critique over a 1 to 5 scale.",
          src: { t: "Hamel Husain · Using LLM-as-a-judge for evaluation", url: "https://hamel.dev/blog/posts/llm-judge/index.html" } },
        { term: "Prompt injection and the lethal trifecta",
          def: "Prompt injection happens when text the agent reads, such as a web page, an issue or a PR comment, contains instructions and the model follows them. No filter catches it reliably. Simon Willison's lethal trifecta: an agent that has access to private data, is exposed to untrusted content, and can communicate externally can be made to leak that data. For every run, make sure at least one of the three is missing. The <a data-cc=\"sandboxing\">sandbox</a> is how you remove the third.",
          src: { t: "Simon Willison · The lethal trifecta (Jun 2025)", url: "https://simonwillison.net/2025/Jun/16/the-lethal-trifecta/" }, cc: "sandboxing" },
        { term: "Permissions and sandboxing",
          def: "Guardrails that work even when the model does not cooperate. An allow/deny list decides which tools and commands can run. An OS-level sandbox limits file writes to the workspace and network access to named hosts. Hooks block specific actions, and step and budget caps stop runaway loops. Anthropic reports that sandboxing cut permission prompts in Claude Code by 84%, because safe commands no longer need a human to approve them.",
          src: { t: "Anthropic · Claude Code sandboxing (Oct 2025)", url: "https://www.anthropic.com/engineering/claude-code-sandboxing" }, cc: "permissions" },
        { term: "A2A (Agent2Agent)",
          def: "An open protocol that lets agents built by different teams discover each other through Agent Cards and exchange tasks. MCP connects an agent to tools and data. A2A connects agents to other agents. It is a Linux Foundation project, and at its one-year mark in April 2026 more than 150 organizations supported it. You will rarely need it inside a single capstone repo.",
          src: { t: "Google Open Source Blog · A year of A2A (Apr 2026)", url: "https://opensource.googleblog.com/2026/04/a-year-of-open-collaboration-celebrating-the-anniversary-of-a2a.html" } }
      ]},

      { type: "h", text: "What changed in the last 12 months", id: "changes" },
      { type: "p", html: "If a tutorial you find predates these changes, check it against the current docs before you copy it." },
      { type: "table", head: ["When", "What changed", "What it means for you", "Source"], rows: [
        ["Sep 2025", "The official MCP Registry launched in preview: an open catalog and API of public MCP servers.", "Look for an existing server before you write one.", "<a href=\"https://blog.modelcontextprotocol.io/posts/2025-09-08-mcp-registry-preview/\" target=\"_blank\" rel=\"noopener\">MCP blog</a>"],
        ["Sep 2025", "Anthropic published \"Effective context engineering\" and \"Writing effective tools for agents\".", "Context engineering and tool design became named skills with published practices.", "<a href=\"https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents\" target=\"_blank\" rel=\"noopener\">Anthropic</a>"],
        ["Oct 2025", "Anthropic introduced Agent Skills and OS-level sandboxing in Claude Code.", "Procedural knowledge moved into skills. Filesystem and network isolation became a default harness part.", "<a href=\"https://www.anthropic.com/engineering/equipping-agents-for-the-real-world-with-agent-skills\" target=\"_blank\" rel=\"noopener\">Anthropic</a>"],
        ["Nov 2025", "Anthropic described harnesses for long-running agents (progress file, feature list, one feature per session) and code execution with MCP.", "Multi-hour tasks became a design problem you solve with files and git.", "<a href=\"https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents\" target=\"_blank\" rel=\"noopener\">Anthropic</a>"],
        ["9 Dec 2025", "The Linux Foundation formed the Agentic AI Foundation (AAIF) with MCP, goose and AGENTS.md as founding projects. AGENTS.md was already used by more than 60,000 open-source projects.", "MCP and AGENTS.md are vendor-neutral, so they are safe to build on.", "<a href=\"https://blog.modelcontextprotocol.io/posts/2025-12-09-mcp-joins-agentic-ai-foundation/\" target=\"_blank\" rel=\"noopener\">MCP blog</a>"],
        ["18 Dec 2025", "Agent Skills became an open standard at agentskills.io. GitHub Copilot added support the same day, and Codex reads the same SKILL.md format.", "A skill you write once works across tools. This is why it is one of your five deliverables.", "<a href=\"https://github.blog/changelog/2025-12-18-github-copilot-now-supports-agent-skills/\" target=\"_blank\" rel=\"noopener\">GitHub changelog</a>"],
        ["Jan 2026", "Anthropic published \"Demystifying evals for AI agents\". MCP Apps became the first official MCP extension, with support in ChatGPT, Claude, goose and VS Code.", "Agent evals now have a shared vocabulary (graders, pass@k, pass^k). MCP tools can return interactive UI.", "<a href=\"https://blog.modelcontextprotocol.io/posts/2026-01-26-mcp-apps/\" target=\"_blank\" rel=\"noopener\">MCP blog</a>"],
        ["Feb 2026", "OpenAI's \"Harness engineering\" post described a product built with no hand-written code, where engineers design the environment and agents write the code.", "The term \"harness engineering\" took off. Your harness is a graded deliverable.", "<a href=\"https://openai.com/index/harness-engineering/\" target=\"_blank\" rel=\"noopener\">OpenAI</a>"],
        ["Mar-Apr 2026", "Anthropic published a planner/generator/evaluator harness for long app builds and Claude Code's classifier-based auto mode. Böckeler published guides and sensors. Cognition revised its stance on multi-agent systems.", "Separate the agent that works from the agent that judges. Keep writes single-threaded.", "<a href=\"https://www.anthropic.com/engineering/harness-design-long-running-apps\" target=\"_blank\" rel=\"noopener\">Anthropic</a>"],
        ["Jul 2026", "MCP spec 2026-07-28: stateless core, extensions framework, authorization hardening (Client ID Metadata Documents preferred for client registration).", "Tutorials that use the old session handshake are out of date. Check the spec version.", "<a href=\"https://blog.modelcontextprotocol.io/posts/2026-07-28/\" target=\"_blank\" rel=\"noopener\">MCP blog</a>"],
        ["Jul 2026", "Copilot code review: support for Agent Skills and MCP became generally available.", "Your skills and MCP servers can run inside a code review bot as well as in your editor.", "<a href=\"https://github.blog/changelog/2026-07-29-copilot-code-review-agent-skills-and-mcp-now-generally-available/\" target=\"_blank\" rel=\"noopener\">GitHub changelog</a>"],
        ["2026", "Claude Code reads AGENTS.md directly (v2.1.277 and later) when there is no CLAUDE.md.", "One rule file can serve Claude Code, Codex, Copilot and Cursor.", "<a href=\"https://code.claude.com/docs/en/memory\" target=\"_blank\" rel=\"noopener\">Claude Code docs</a>"]
      ]},

      { type: "h", text: "Essential reading (week 1)", id: "reading" },
      { type: "resources", items: [
        { kind: "read", t: "Effective context engineering for AI agents", by: "Anthropic", date: "Sep 2025", url: "https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents", note: "Start here. It covers the attention budget, just-in-time retrieval, compaction, note-taking and subagents in one post." },
        { kind: "read", t: "Harness engineering for coding agent users", by: "Birgitta Böckeler · martinfowler.com", date: "Apr 2026", url: "https://martinfowler.com/articles/harness-engineering.html", note: "The guides-and-sensors model. Use its vocabulary when you describe your harness in the report." },
        { kind: "read", t: "Harness engineering", by: "Ryan Lopopolo · OpenAI", date: "Feb 2026", url: "https://openai.com/index/harness-engineering/", note: "A team shipping a product whose code was all written by Codex. Read it for the practices: give the agent a map, not a manual." },
        { kind: "read", t: "Demystifying evals for AI agents", by: "Anthropic", date: "Jan 2026", url: "https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents", note: "Grader types, pass@k vs pass^k, and why you start with 20 to 50 tasks. Your eval suite follows this." },
        { kind: "read", t: "Writing effective tools for agents, with agents", by: "Anthropic", date: "Sep 2025", url: "https://www.anthropic.com/engineering/writing-tools-for-agents", note: "Read before you define your first tool. It explains how names, descriptions and response size change agent accuracy." },
        { kind: "read", t: "Harness design for long-running application development", by: "Prithvi Rajasekaran · Anthropic", date: "Mar 2026", url: "https://www.anthropic.com/engineering/harness-design-long-running-apps", note: "A planner, a generator and a separate evaluator, with costs. It also argues for removing scaffolding when a new model no longer needs it." },
        { kind: "read", t: "Multi-agents: what's actually working", by: "Walden Yan · Cognition", date: "Apr 2026", url: "https://cognition.com/blog/multi-agents-working", note: "Read before you split your system into several agents. It explains which multi-agent patterns work in production and which do not." },
        { kind: "read", t: "The lethal trifecta for AI agents", by: "Simon Willison", date: "Jun 2025", url: "https://simonwillison.net/2025/Jun/16/the-lethal-trifecta/", note: "Short. After reading it, you should be able to say which leg of the trifecta your project removes." }
      ]},

      { type: "h", text: "Watch", id: "watch" },
      { type: "resources", items: [
        { kind: "video", t: "From vibe coding to agentic engineering", by: "Andrej Karpathy with Stephanie Zhan · Sequoia AI Ascent", date: "Apr 2026", url: "https://www.youtube.com/watch?v=96jN2OCOfLs", note: "What changed when agents became reliable enough for long, coherent tasks, and what engineers do now." },
        { kind: "video", t: "We cut 80% of Claude Code's prompt", by: "Boris Cherny · YC Startup School", date: "2026", url: "https://www.youtube.com/watch?v=qyPCVqFUyDo", note: "Claude Code's creator on removing harness instructions a newer model no longer needs, and on letting the agent verify its own work." },
        { kind: "video", t: "Don't build agents, build skills instead", by: "Barry Zhang and Mahesh Murag · Anthropic, AI Engineer", date: "Dec 2025", url: "https://www.youtube.com/watch?v=CEvIs9y1uog", note: "Why skills exist and how they are structured. Watch before you package yours." },
        { kind: "video", t: "No vibes allowed: solving hard problems in complex codebases", by: "Dex Horthy · HumanLayer, AI Engineer Code Summit", date: "2025", url: "https://www.youtube.com/watch?v=rmvDxxNubIg", note: "Research, plan, implement, with intentional compaction between steps. Context engineering applied to a large codebase." },
        { kind: "video", t: "Why AI evals are the hottest new skill for product builders", by: "Hamel Husain and Shreya Shankar · Lenny's Podcast", date: "Sep 2025", url: "https://www.youtube.com/watch?v=BsWxPI9UM4c", note: "Error analysis, open and axial coding, and when to use code-based checks or an LLM judge." },
        { kind: "video", t: "AI Engineer channel (World's Fair 2026 talks)", by: "AI Engineer", date: "Jul 2026", url: "https://www.youtube.com/@aiDotEngineer", note: "The 2026 World's Fair had tracks on context engineering, harness engineering, evals, memory and sandboxes. Pick one talk per track." }
      ]},

      { type: "h", text: "Courses (free)", id: "courses" },
      { type: "resources", items: [
        { kind: "course", t: "Introduction to agent skills", by: "Anthropic Academy", url: "https://anthropic.skilljar.com/introduction-to-agent-skills", note: "Write a SKILL.md, write descriptions that trigger reliably, share skills via plugins. About the size of your skill deliverable." },
        { kind: "course", t: "Introduction to subagents", by: "Anthropic Academy", url: "https://anthropic.skilljar.com/introduction-to-subagents", note: "When a subagent helps and how to define one in Claude Code." },
        { kind: "course", t: "Introduction to Model Context Protocol", by: "Anthropic Academy", url: "https://anthropic.skilljar.com/introduction-to-model-context-protocol", note: "Build an MCP server and client in Python with tools, resources and prompts." },
        { kind: "course", t: "Claude Code in Action", by: "Anthropic Academy", url: "https://anthropic.skilljar.com/claude-code-in-action", note: "Hands-on use of the harness you will study in the Claude Code section." },
        { kind: "course", t: "Agent Skills with Anthropic", by: "DeepLearning.AI · Elie Schoppik", date: "2026", url: "https://www.deeplearning.ai/courses/agent-skills-with-anthropic", note: "Skills across Claude Code, the API and the Agent SDK, and how they combine with MCP and subagents." },
        { kind: "course", t: "Agentic AI", by: "DeepLearning.AI · Andrew Ng", date: "Oct 2025", url: "https://www.deeplearning.ai/courses/agentic-ai", note: "Reflection, tool use, planning and multi-agent patterns, with a strong module on evals and error analysis." },
        { kind: "course", t: "Evaluating AI Agents", by: "DeepLearning.AI with Arize", url: "https://www.deeplearning.ai/courses/evaluating-ai-agents", note: "Tracing an agent and choosing code-based, LLM-judge or human evaluators per component." },
        { kind: "course", t: "The Context Course", by: "Hugging Face", date: "2026", url: "https://huggingface.co/learn/context-course/unit0/introduction", note: "Six units: skills, MCP, plugins, subagents, hooks, then a minimal agent loop from scratch. Close to this guide's scope." },
        { kind: "course", t: "MCP Course", by: "Hugging Face", url: "https://huggingface.co/learn/mcp-course/en/unit0/introduction", note: "MCP concepts and building servers, with free certificates." }
      ]},

      { type: "h", text: "Background (older but foundational)", id: "background" },
      { type: "resources", items: [
        { kind: "read", t: "Building effective agents", by: "Anthropic", date: "Dec 2024", url: "https://www.anthropic.com/engineering/building-effective-agents", note: "Workflow patterns (chaining, routing, parallelization, orchestrator-workers, evaluator-optimizer) and when to use an agent at all." },
        { kind: "read", t: "Don't build multi-agents", by: "Walden Yan · Cognition", date: "Jun 2025", url: "https://cognition.com/blog/dont-build-multi-agents", note: "The original argument about shared context. Read it with the April 2026 update above." },
        { kind: "read", t: "Claude Code: best practices for agentic coding", by: "Anthropic", date: "Apr 2025", url: "https://www.anthropic.com/engineering/claude-code-best-practices", note: "Explore, plan, code, commit. Some details are dated. The current version lives in the Claude Code docs." },
        { kind: "read", t: "AI evals: everything you need to know (FAQ)", by: "Hamel Husain and Shreya Shankar", date: "2025", url: "https://hamel.dev/blog/posts/evals-faq/", note: "Answers to the questions you will have in week 3." }
      ]},

      { type: "lab", id: "lab-workflow-vs-agent", title: "Explain it with code: workflow vs agent", time: "2 h", level: "Core",
        goal: "Answer the same 10 questions about a small grades table two ways: a fixed workflow (one LLM call picks a query, code runs it) and an agent loop (the model calls tools until it has an answer). Measure correctness, LLM calls and tokens for both. You will be able to say, with your own numbers, what an agent costs and what it buys you.",
        build: [
          "Install the client: <code>pip install openai</code>. Any OpenAI-compatible endpoint whose model supports tool calling works. Options include local Ollama (<code>BASE_URL=http://localhost:11434/v1</code>) or a free-tier API from the Tools page. Set <code>BASE_URL</code>, <code>MODEL</code> and, if needed, <code>API_KEY</code>.",
          "Save the script below as <code>lab.py</code>. It builds a fixed dataset of 20 students (groups A to D, seeded so everyone gets the same data), 10 questions with known answers, two tools (<code>list_groups</code>, <code>query</code>), a <code>workflow()</code> and an <code>agent()</code>.",
          "Before you run anything, write down your prediction: which questions will the workflow fail, and how many LLM calls will the agent use in total?",
          "Run <code>python lab.py workflow</code>, then <code>python lab.py agent</code>. Copy the last line of each into a table with columns: mode, correct, LLM calls, tokens.",
          "Run the agent two more times. Record whether any question flips between PASS and FAIL. That is the non-determinism your eval suite will have to handle (pass^k).",
          "Write 5 sentences in your notes: where the workflow failed and why, what the agent's extra calls bought, what it cost in tokens, and which design you would ship for this task."
        ],
        verify: [
          "Data check: <code>python -c \"import lab; print(lab.query('D','mean'), lab.query('ALL','count'), lab.list_groups())\"</code> prints <code>13.2 20 ['A', 'B', 'C', 'D']</code> (with <code>BASE_URL</code> and <code>MODEL</code> set). If it does not, your dataset differs and the expected answers will not match.",
          "Workflow: the last line reports exactly <code>10 LLM calls</code>. The questions <em>Which group has the highest mean grade?</em>, <em>highest minus lowest</em> and <em>Which group has the lowest mean grade?</em> cannot be expressed as a single query, so expect FAIL on those three and at most 7/10 correct.",
          "Agent: every question uses at least 2 LLM calls (one tool call, one answer). The two <em>Which group</em> questions need at least one query per group. With a capable model expect 9 or 10 correct, and more total calls and tokens than the workflow.",
          "Expected answers, if you need to debug a FAIL line: 20, 11.8, 16, 8, 5, 11.8, 17, D, 10, B.",
          "Your notes contain the results table and your prediction, and the difference between the two is explained."
        ],
        code: { lang: "py", title: "lab.py (tested against a mock OpenAI-compatible server)", text: "import json, os, random, re, sys\nfrom openai import OpenAI\n\nclient = OpenAI(base_url=os.environ[\"BASE_URL\"], api_key=os.environ.get(\"API_KEY\", \"none\"))\nMODEL = os.environ[\"MODEL\"]\nrandom.seed(7)\nROWS = [{\"name\": f\"s{i:02d}\", \"group\": \"ABCD\"[i % 4], \"grade\": random.randint(8, 19)} for i in range(20)]\nCASES = [(\"How many students are there in total?\", 20), (\"What is the mean grade of group A?\", 11.8),\n         (\"What is the highest grade in group C?\", 16), (\"What is the lowest grade across all students?\", 8),\n         (\"How many students are in group D?\", 5), (\"What is the mean grade across all students?\", 11.8),\n         (\"What is the highest grade in group B?\", 17), (\"Which group has the highest mean grade?\", \"D\"),\n         (\"What is the highest grade minus the lowest grade, over all students?\", 10),\n         (\"Which group has the lowest mean grade?\", \"B\")]\n\ndef list_groups():\n    return sorted({r[\"group\"] for r in ROWS})\n\ndef query(group, op):\n    g = [r[\"grade\"] for r in ROWS if group in (\"ALL\", r[\"group\"])]\n    return round({\"mean\": sum(g) / len(g), \"max\": max(g), \"min\": min(g), \"count\": len(g)}[op], 2)\n\nFUNCS = {\"list_groups\": list_groups, \"query\": query}\nQUERY_PARAMS = {\"type\": \"object\", \"required\": [\"group\", \"op\"], \"properties\": {\n    \"group\": {\"type\": \"string\", \"description\": \"A group name, or ALL for every student\"},\n    \"op\": {\"type\": \"string\", \"enum\": [\"mean\", \"max\", \"min\", \"count\"]}}}\nTOOLS = [{\"type\": \"function\", \"function\": {\"name\": \"list_groups\", \"description\": \"List the group names.\",\n                                           \"parameters\": {\"type\": \"object\", \"properties\": {}}}},\n         {\"type\": \"function\", \"function\": {\"name\": \"query\", \"description\": \"One statistic over student grades.\",\n                                           \"parameters\": QUERY_PARAMS}}]\n\ndef call(msgs, **kw):\n    r = client.chat.completions.create(model=MODEL, messages=msgs, temperature=0, **kw)\n    return r.choices[0].message, (r.usage.total_tokens if r.usage else 0)\n\ndef workflow(question):  # fixed chain: one LLM call picks the query, code runs it\n    msg, tok = call([{\"role\": \"system\", \"content\": \"Reply with JSON only, matching: \" + json.dumps(QUERY_PARAMS)},\n                     {\"role\": \"user\", \"content\": question}])\n    try:\n        args = json.loads(re.search(r\"\\{.*\\}\", msg.content, re.S).group())\n        return str(query(args[\"group\"], args[\"op\"])), 1, tok\n    except Exception:\n        return \"ERROR\", 1, tok\n\ndef agent(question, max_steps=8):  # loop: the model picks the next tool until it answers\n    msgs = [{\"role\": \"system\", \"content\": \"Use the tools. End with the final answer only.\"},\n            {\"role\": \"user\", \"content\": question}]\n    tokens = 0\n    for step in range(1, max_steps + 1):\n        msg, tok = call(msgs, tools=TOOLS)\n        tokens += tok\n        if not msg.tool_calls:\n            return msg.content or \"\", step, tokens\n        msgs.append({\"role\": \"assistant\", \"content\": msg.content,\n                     \"tool_calls\": [tc.model_dump() for tc in msg.tool_calls]})\n        for tc in msg.tool_calls:\n            out = FUNCS[tc.function.name](**json.loads(tc.function.arguments or \"{}\"))\n            msgs.append({\"role\": \"tool\", \"tool_call_id\": tc.id, \"content\": json.dumps(out)})\n    return \"MAX_STEPS\", max_steps, tokens\n\ndef correct(answer, expected):\n    if isinstance(expected, str):\n        return re.search(r\"\\b%s\\b\" % expected, answer) is not None\n    return any(abs(float(n) - expected) < 0.01 for n in re.findall(r\"-?\\d+(?:\\.\\d+)?\", answer))\n\nif __name__ == \"__main__\":\n    mode = sys.argv[1]\n    ok = calls = toks = 0\n    for q, expected in CASES:\n        ans, n, t = {\"workflow\": workflow, \"agent\": agent}[mode](q)\n        good = correct(ans, expected)\n        ok, calls, toks = ok + good, calls + n, toks + t\n        print(\"PASS\" if good else \"FAIL\", n, t, q, \"->\", ans.strip()[:60])\n    print(f\"{mode}: {ok}/{len(CASES)} correct, {calls} LLM calls, {toks} tokens\")\n" },
        stretch: [
          "Change the <code>query</code> tool description to something vague (\"does stuff\") and rerun the agent. Record the change in accuracy. That is the tool-design lesson in one experiment.",
          "Add a third mode, a routed workflow: one LLM call classifies the question as single-query or compare-groups, then code runs the right fixed path. Compare its calls and accuracy with the other two.",
          "Context rot mini-experiment: add 2,000 rows of irrelevant text to the system prompt of <code>agent()</code> and rerun. Record correctness and tokens."
        ],
        links: [
          { t: "Anthropic · Building effective agents", url: "https://www.anthropic.com/engineering/building-effective-agents" },
          { t: "Anthropic · Writing effective tools for agents", url: "https://www.anthropic.com/engineering/writing-tools-for-agents" },
          { t: "Claude Code docs · How Claude Code works", url: "https://code.claude.com/docs/en/how-claude-code-works" }
        ],
        cc: ["agent-loop", "tools", "context", "cost-tracking"] },

      { type: "checklist", id: "concepts-ready", title: "You are ready when you can explain, without notes", items: [
        "The difference between a workflow and an agent, using the numbers from your lab (correct answers, LLM calls, tokens).",
        "Context rot, and three techniques that limit it (for example compaction, subagents, just-in-time retrieval).",
        "Your project's harness in Böckeler's terms: at least two guides and two sensors.",
        "What loads into context at startup for a rule file and what loads for a skill, and why that difference matters.",
        "What MCP standardizes, which MCP server your project uses (if any), and how that server is authenticated.",
        "Which leg of the lethal trifecta your project removes, and the mechanism that removes it (sandbox, permissions or a hook).",
        "When a multi-agent design is worth its token cost, with the 90.2% and 15x figures from Anthropic's research system.",
        "What your eval suite measures, how many items it has, and how you will check your LLM judge (true positive and true negative rates)."
      ]}
    ]
  };

  P.primitives = {
    id: "primitives",
    num: "06",
    title: "Common building blocks",
    kicker: "Applies to every project",
    summary: "The five artifacts every team produces, whatever the project, each with a command that proves it works, so cross-team peer review is possible.",
    blocks: [
      { type: "lead", html: "Every project uses the same five building blocks. Start them in week 1. They carry <strong>40% of the grade</strong> through the \"Context and harness\" and \"Eval discipline\" criteria. Each one has a test you can run, listed after the layout." },
      { type: "primitives", items: [
        { t: "Rule file: AGENTS.md",
          d: "Short, versioned instructions that every session loads: build and test commands, conventions, always and never rules, what to do when unsure. Keep it under 200 lines. Put task-specific procedures in a skill and hard limits in permissions or hooks. Add a line only in response to a failure you actually saw.",
          done: "It changed at least 3 times, each commit message links the eval run or trace that motivated the change, and a fresh session answers 3 probe questions about your rules correctly.",
          src: { t: "Claude Code docs · CLAUDE.md and AGENTS.md", url: "https://code.claude.com/docs/en/memory" },
          cc: ["claude-md", "memory", "context"] },
        { t: "At least one packaged Skill",
          d: "A folder with SKILL.md (name, description, instructions) plus optional scripts/ and references/ that captures one procedure your agent or your team repeats, for example triaging a failing eval item or writing a review comment in your format. The description decides when the skill loads, so write it as \"Use when ...\". Follow the open standard so it works in Claude Code, Codex and Copilot.",
          done: "It triggers on at least 4 of 5 prompts that should use it and on none of 5 that should not, and it works unchanged when copied into a second repo.",
          src: { t: "Agent Skills specification", url: "https://agentskills.io/home" },
          cc: ["skills", "plugins", "slash-commands"] },
        { t: "A harness",
          d: "Four parts, wired in from week 1. Tool scoping: every tool and command the agent may use is on an explicit allow list. Sandbox: anything that runs code can write only inside the workspace and reach only named hosts. Guardrails: deterministic checks such as hooks, validators, step caps and budget caps that do not rely on the model agreeing. Observability: every run leaves a trace with steps, tokens, cost and latency.",
          done: "Each guardrail has an automated test that attempts the forbidden action and shows it blocked, and you can open the trace for any eval item listed in RESULTS.md.",
          src: { t: "Anthropic · Claude Code sandboxing (Oct 2025)", url: "https://www.anthropic.com/engineering/claude-code-sandboxing" },
          cc: ["permissions", "sandboxing", "hooks", "settings", "cost-tracking"] },
        { t: "An eval suite (30+ items)",
          d: "At least 30 items stored in the repo (input, expected outcome, tags), taken from real failures where possible, run with one command that prints a score table. Use code-based checks first. Use an LLM judge only for what code cannot check, and validate it against your own labels. Run each item more than once, because the agent is not deterministic.",
          done: "RESULTS.md has at least 3 dated rows (baseline, mid-project, final), each with the commit hash, model, score, cost, and the judge's true positive and true negative rates if you use a judge. CI runs the suite on every pull request.",
          src: { t: "Anthropic · Demystifying evals for AI agents (Jan 2026)", url: "https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents" },
          cc: ["headless", "github-actions"] },
        { t: "A written post-mortem",
          d: "What worked, what broke, what you would change, and what it cost to run. Use numbers and links, not adjectives. Anthropic's April 2026 Claude Code postmortem is a good model: three issues, each with a cause, the dates it was live, and the fix.",
          done: "Every claim links to a RESULTS.md row, a trace or a commit, and another team can reproduce your final score by following the README.",
          src: { t: "Anthropic · Claude Code quality postmortem (Apr 2026)", url: "https://www.anthropic.com/engineering/april-23-postmortem" },
          cc: ["cost-tracking"] }
      ]},

      { type: "callout", tone: "info", title: "AGENTS.md or CLAUDE.md?", html: "Write <code>AGENTS.md</code>. Codex, Copilot, Cursor and other tools read it, and Claude Code v2.1.277 and later reads it directly when there is no <code>CLAUDE.md</code> in the working directory or above it. If a <code>CLAUDE.md</code> exists, Claude Code reads only that file by default. In that case, put <code>@AGENTS.md</code> on the first line of <code>CLAUDE.md</code> and add Claude-specific notes below it. A symlink (<code>ln -s AGENTS.md CLAUDE.md</code>) also works, but it breaks on Windows clones unless <code>core.symlinks</code> is on, so prefer the import. To check what loaded, run <code>/memory</code> or <code>/context</code> in Claude Code. See <a data-cc=\"claude-md\">CLAUDE.md in the Claude Code section</a> and the <a href=\"https://code.claude.com/docs/en/memory\" target=\"_blank\" rel=\"noopener\">official docs</a>." },

      { type: "h", text: "Suggested repository layout", id: "layout" },
      { type: "code", lang: "text", title: "your-project/", text: "your-project/\n├── AGENTS.md                  # rule file for every coding agent (under 200 lines)\n├── CLAUDE.md                  # optional: \"@AGENTS.md\" + Claude-only notes\n├── README.md                  # what it is, one-command setup, models and limits used\n├── ARCHITECTURE.md            # diagram + hand-off contracts between components\n├── .claude/\n│   ├── settings.json          # permissions allow/deny, hooks, sandbox (committed)\n│   ├── skills/<skill-name>/   # SKILL.md + scripts/ + references/\n│   └── agents/<name>.md       # subagent definitions, if you use any\n├── .agents/skills -> ../.claude/skills   # symlink so Codex finds the same skills\n├── .mcp.json                  # project MCP servers, if any (no secrets)\n├── src/                       # your agent or app\n├── tests/\n│   └── test_guardrails.py     # one test per guardrail: attempt it, assert blocked\n├── evals/\n│   ├── dataset.jsonl          # 30+ items: input, expected outcome, tags\n│   ├── run.py                 # one command, prints a score table\n│   ├── judge.md               # LLM-judge prompt + its TPR/TNR on your labels\n│   └── RESULTS.md             # dated rows: baseline -> mid -> final, with commit hash\n├── .github/workflows/evals.yml  # runs tests + evals on every pull request\n├── .env.example               # key names only, never values\n└── docs/postmortem.md" },
      { type: "p", html: "Claude Code reads skills from <code>.claude/skills/</code> and not from <code>.agents/skills/</code>. Codex looks in <code>.agents/skills/</code>. The symlink lets both tools find one copy. Subagents go in <code>.claude/agents/</code> (see <a data-cc=\"subagents\">subagents</a>), and project MCP servers go in <code>.mcp.json</code> (see <a data-cc=\"mcp\">MCP</a>)." },

      { type: "h", text: "How to test each building block", id: "tests" },
      { type: "table", head: ["Building block", "Command or action", "Passes when"], rows: [
        ["Rule file", "Start a fresh Claude Code session and run <code>/memory</code>, then ask 3 probe questions whose answers are only in <code>AGENTS.md</code> (for example \"what command runs the evals?\"). For other tools, run the same probes.", "The file is listed as loaded and all 3 answers match the file."],
        ["Skill", "Invoke it directly with <code>/&lt;skill-name&gt;</code>. Then send 5 prompts that should trigger it and 5 that should not, and check the transcript for which ones loaded it. Copy the folder into a second repo and repeat.", "Direct invocation works, at least 4 of 5 trigger, 0 of 5 false triggers, and the same results in the second repo."],
        ["Harness: tool scoping and guardrails", "<code>pytest tests/test_guardrails.py</code>. Each test asks the agent, or calls the hook directly, to do one forbidden thing: write outside the workspace, run a denied command, exceed the step cap.", "Every test passes, and each one fails if you disable its guardrail. Check that once, so you know the tests can fail."],
        ["Harness: sandbox", "From inside the sandbox, try <code>curl</code> to a host that is not on your allow list and try to write to <code>~/</code>.", "Both are refused, and the refusal shows up in the trace."],
        ["Harness: observability", "Run one eval item and open its trace.", "You can see every model call and tool call with tokens, cost and latency."],
        ["Eval suite", "<code>python evals/run.py</code> (or <code>make evals</code>) locally. In CI, <code>.github/workflows/evals.yml</code> runs it on every pull request.", "A score table prints, a new dated row lands in RESULTS.md, and CI shows the score on the pull request."],
        ["Post-mortem", "Give it to another team and ask them to reproduce your final score from the README and to check 5 claims against the links.", "They reproduce the score within your stated variance, and all 5 claims check out."]
      ]},
      { type: "callout", tone: "tip", title: "Run the agent in CI", html: "To test an agent built on Claude Code in CI, use <a data-cc=\"headless\">headless mode</a> (<code>claude -p</code>) or the <a data-cc=\"github-actions\">Claude Code GitHub Action</a>. Pin the model in the workflow file (for example with <code>--model</code>) so that score changes come from your changes and not from a silent default change." }
    ]
  };
})();
